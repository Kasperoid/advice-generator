import styled from 'styled-components';
import { baseTheme } from '../theme';

export const DividerStickStyled = styled.div`
  width: 5px;
  height: 13px;
  background-color: ${baseTheme.color.primary};
  border-radius: 25%;
`;
